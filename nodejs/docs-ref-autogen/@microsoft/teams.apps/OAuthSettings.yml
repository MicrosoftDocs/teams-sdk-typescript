### YamlMime:TSTypeAlias
name: OAuthSettings
uid: '@microsoft/teams.apps.OAuthSettings'
package: '@microsoft/teams.apps'
summary: OAuth configuration for an app.
fullName: OAuthSettings
remarks: ''
isDeprecated: true
customDeprecatedMessage: |-
  Register connections with `app.addOAuthFlow(...)` and use the
  returned flow for token lookup, sign-in, status, and sign-out operations.
  This configuration remains supported for legacy context OAuth behavior.
syntax: |
  type OAuthSettings = {
    defaultConnectionName?: string
    fetchUserToken?: boolean
  }
